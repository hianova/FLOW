/*
 * FLOW Native Doc-as-Topology Sync Generator
 * Auto-synchronizes flow-book docs into src/generated_book_knowledge.h
 */
#include <stdio.h>
#include <stdlib.h>

int main(int argc, char **argv) {
    const char *out_path = (argc > 1) ? argv[1] : "src/generated_book_knowledge.h";
    FILE *f = fopen(out_path, "w");
    if (!f) {
        perror("fopen");
        return 1;
    }

    fputs("/* AUTO-GENERATED BY tools/sync-flow-book.c - DO NOT EDIT MANUALLY */\n", f);
    fputs("#ifndef FLOW_GENERATED_BOOK_KNOWLEDGE_H\n", f);
    fputs("#define FLOW_GENERATED_BOOK_KNOWLEDGE_H\n\n", f);
    fputs("#include \"flowy.h\"\n", f);
    fputs("#include <string.h>\n\n", f);
    fputs("#define FLOW_BOOK_CHAPTER_COUNT 18\n\n", f);
    fputs("typedef struct {\n", f);
    fputs("    const char *chapter_ref;\n", f);
    fputs("    const char *chapter_title;\n", f);
    fputs("    const char *philosophy_why;\n", f);
    fputs("    const char *book_excerpt;\n", f);
    fputs("} FlowBookChapterDoc;\n\n", f);
    fputs("static const FlowBookChapterDoc FLOW_BOOK_CHAPTERS_ZH[FLOW_BOOK_CHAPTER_COUNT] = {\n", f);
    fputs("    {\n", f);
    fputs("        .chapter_ref = \"ch01_what_is_flow.md\",\n", f);
    fputs("        .chapter_title = \"第一章：什麼是 FLOW？ (從靜態編譯到動態活體系統的典範轉移)\",\n", f);
    fputs("        .philosophy_why = \"我們不是在編譯一份死沉沉的程式碼，而是在孕育一個能夠感知環境、自我退火、在毀滅性壓力下自我蛻變的拓樸活體。\",\n", f);
    fputs("        .book_excerpt = \"自 1957 年 Fortran 編譯器問世以來，編譯器架構的核心假設始終如一：原始碼是唯一真理。FLOW 徹底顛覆此一假設，將代碼視為可感知環境與持續演化的活體幾何流形。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .chapter_ref = \"ch02_intent_vs_implementation.md\",\n", f);
    fputs("        .chapter_title = \"第二章：意圖規格 .flow (我們只向宇宙宣告幾何約束，不寫邏輯)\",\n", f);
    fputs("        .philosophy_why = \"在 FLOW 的哲學中，撰寫程式碼不是告訴 CPU 如何一步一步執行指令，而是向宇宙宣告系統必須服從的幾何邊界與不變量。\",\n", f);
    fputs("        .book_excerpt = \"一個標準的 .flow 描述檔是純宣告式的，不包含命令式迴圈或指標操作。開發者只宣告 input 規模、flow 流水線與物理約束，實作由引擎自動坍縮合成。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .chapter_ref = \"ch03_topology_graph.md\",\n", f);
    fputs("        .chapter_title = \"第三章：拓樸圖譜 (將程式碼降維成可推算的依賴約束流形)\",\n", f);
    fputs("        .philosophy_why = \"軟體的架構不是文字檔目錄的堆疊，而是一張高維拓樸圖。在 FLOW 中，程式碼被降維成可直接進行圖論運算、親和性分析與遙測附著的活體神經圖譜。\",\n", f);
    fputs("        .book_excerpt = \"FLOW 將核心模組、驅動、元件與意圖統構為 FlowTopologyGraph，實施嚴格的分層防火牆審計、動態神經遙測附著與零跨層滲漏保證。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .chapter_ref = \"ch04_jet_bundle_phase_space.md\",\n", f);
    fputs("        .chapter_title = \"第四章：噴流束與辛幾何相空間 (.fjet、Hamiltonian 能量守恆與 Mori-Zwanzig 記憶核耗散)\",\n", f);
    fputs("        .philosophy_why = \"在微分流形上，時間不是離散的定時器，而是相空間軌跡的幾何流。消滅熱路徑上的命令式防禦檢查，交由保辛力學與非光滑法錐反作用力接管。\",\n", f);
    fputs("        .book_excerpt = \".fjet 噴流束將系統建模於辛流形 (q, p)，Velocity-Verlet 二階積分嚴格守恆相積。雙曲勢能屏障與 Moreau 凸法錐斥力取代了所有命令式 if 截斷，Mori-Zwanzig 記憶核在 3.2ms 內平息高頻非馬可夫衝擊。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .chapter_ref = \"ch05_discrete_time_crystal.md\",\n", f);
    fputs("        .chapter_title = \"第五章：Floquet 離散時間晶體 (DTC 週期 2T 鎖定、時間平移對稱破缺與非熱化剛性)\",\n", f);
    fputs("        .philosophy_why = \"在混沌噪聲的熱寂深淵中，時間晶體自發打破時間平移對稱性，以次諧波 2T 剛性鎖定系統節拍，免疫一切無序擾動。\",\n", f);
    fputs("        .book_excerpt = \"Floquet 週期驅動誘導離散時間平移對稱破缺 (DTSB)，形成非熱化多體局域化 (MBL) 剛性。次諧波峰值比超過 0.999，在百萬次震盪中維持零能量漂移，為具身協同提供拓撲時鐘。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .chapter_ref = \"ch06_smt_formal_verification.md\",\n", f);
    fputs("        .chapter_title = \"第六章：形式化最高法院 (SMT 4 大定理 QF_LIA UNSAT 證明與 Curry-Howard 死碼消除)\",\n", f);
    fputs("        .philosophy_why = \"啟發式搜尋可以天馬行空，但發射出的每一行機器碼必須擁有無可爭辯的數學證明。SMT 定理證明器是 FLOW 宇宙的最高法院，凡無證明者，一律否決。\",\n", f);
    fputs("        .book_excerpt = \"SMT 最高法院透過 QF_LIA 理論驗證緩衝區邊界、記憶體配額上限、分片隔離與確定性 4 大定理。合約檢查與硬體邊界在編譯期即刻生成 1-Cycle 多面體位元修剪遮罩，下游防禦分支全部作為死碼消除。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .chapter_ref = \"ch07_bmf_coordinate_canvas.md\",\n", f);
    fputs("        .chapter_title = \"第七章：BMF 離散座標畫布 (從隨機退火到 1-Bit 座標畫布、代數流形交集與上位連鎖)\",\n", f);
    fputs("        .philosophy_why = \"隨機退火搜尋只是開拓期的過渡工具；現代 FLOW 將 BMF 定位為離散座標畫布與流形代數交集，以多面體求解與連鎖群翻轉跨越上位效應壁壘。\",\n", f);
    fputs("        .book_excerpt = \"64-bit 狀態脊椎不再依賴盲目隨機突變，而是連續約束流形向硬體暫存器投影的布林快照。流形代數在多面體交集邊界萃取連鎖基因群，以超級位元 (Super-Bit) 原子翻轉破除上位效應。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .chapter_ref = \"ch08_six_mathematical_pillars.md\",\n", f);
    fputs("        .chapter_title = \"第八章：六大數學支柱：經驗啟發式的全息消解 (多面體模型、OCO、Lyapunov、勢能博弈、Moreau 掃掠、單純同調論)\",\n", f);
    fputs("        .philosophy_why = \"凡充斥經驗常數與魔術數字之處，皆是脆弱的避難所。FLOW 以六大數學支柱徹底取代工程猜測，全系統收斂至堅實的數學定理下限。\",\n", f);
    fputs("        .book_excerpt = \"Presburger 多面體模型求解最優 loop tiling，在線凸最佳化 (OCO) 雙對偶影子價格消除快取掃描，Lyapunov 壓縮映射消除盲目重試，勢能博弈收斂至 Wardrop 均衡，Moreau 法錐吞噬微震，單純同調論引導漏洞挖掘。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .chapter_ref = \"ch09_embodied_10khz_reflex.md\",\n", f);
    fputs("        .chapter_title = \"第九章：10kHz 具身反射與非光滑接觸力學 (Moreau 凸集法錐、庫侖摩擦錐、ZMP 與阻抗控制)\",\n", f);
    fputs("        .philosophy_why = \"軟體工程的終極審判不在記憶體與暫存器之中，而在機器人手臂接觸實體物理世界的那一微秒。\",\n", f);
    fputs("        .book_excerpt = \"10kHz 脊髓反射熱迴路徹底拔除 null 檢查與硬截斷，交由 Moreau 閉區間凸錐投影 fmin/fmax 接管，衝擊吸收耗時收斂至 3.20ms。庫侖摩擦錐動態調壓與 ZMP 穩定多邊形形式化保證零打滑、零斷裂。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .chapter_ref = \"ch10_hardware_primitive_drivers.md\",\n", f);
    fputs("        .chapter_title = \"第十章：硬體原語驅動 (SocketCAN/CAN-FD 3-函數極簡 ABI 與微秒級 SMT 搶佔仲裁)\",\n", f);
    fputs("        .philosophy_why = \"Plugin 不該是沉重的編譯器外掛，而只是大腦接在物理世界的視神經與肌肉。奧坎剃刀切除了一切非必要的 24 個回呼實體，只留下極簡的 3 個硬體原語驅動介面。\",\n", f);
    fputs("        .book_excerpt = \"極簡驅動 ABI 只需宣告硬體原語、呈報 SMT 物理邊界與執行調度。SocketCAN / CAN-FD 原生驅動實現微秒級搶佔仲裁，SMT 定理嚴格證明急停延遲不超過 300 微秒。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .chapter_ref = \"ch11_jit_and_geometric_morphing.md\",\n", f);
    fputs("        .chapter_title = \"第十一章：JIT 代碼發射與幾何變形 (AoS 到 SoA 即時重映射與生存模式)\",\n", f);
    fputs("        .philosophy_why = \"程式碼不是雕刻在石頭上的死文字，而是能在記憶體中自發變形的黏土。當記憶體即將崩潰時，系統在微秒內完成拓樸幾何變形，躲過 OS OOM Killer 的致命屠刀。\",\n", f);
    fputs("        .book_excerpt = \"JIT 發射器支援 C、Rust、Python 與 LLVM IR 多目標。透過 mremap 實現 AoS 與 SoA 零拷貝即時切換；在極端低記憶體下自我否決 JIT，遁入零分配 Static Survival 避難所。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .chapter_ref = \"ch12_wavefront_cacheline_topology.md\",\n", f);
    fputs("        .chapter_title = \"第十二章：Wavefront 波前環與 alignas(64) 快取行約束 (單快取行 64B 撕裂無關原子相位切換)\",\n", f);
    fputs("        .philosophy_why = \"在多核心並發的世界裡，跨快取行撕裂即原罪。FLOW 將波前槽約束在單一 64 位元組快取行內，以原子相位切換實現零自旋鎖同步。\",\n", f);
    fputs("        .book_excerpt = \"FlowWavefrontSlot 嚴格遵循 alignas(64) 硬體快取行約束，消滅 False Sharing 與跨行撕裂。單快取行 64B 原子相位切換與排空視界定理保證無鎖並發安全。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .chapter_ref = \"ch13_qsbr_lockfree_hotswap.md\",\n", f);
    fputs("        .chapter_title = \"第十三章：QSBR 零鎖熱替換 (微秒級世代指針遷移與 Watchdog 隔離)\",\n", f);
    fputs("        .philosophy_why = \"在每秒數千萬次請求的高並發伺服器中，獲取哪怕一把讀寫鎖都會造成災難性的快取一致性風暴。FLOW 採用統一 QSBR 無鎖架構，實現了讀取路徑零原子寫入、熱替換微秒級無損遷移。\",\n", f);
    fputs("        .book_excerpt = \"QSBR 透過靜態世代演進實現 >390M ops/s 讀取吞吐量。看門狗以 mprotect 隔離掉隊讀者執行緒，以客製化信號捕捉保證優雅降級與 0 丟失請求熱替換。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .chapter_ref = \"ch14_fvec_universal_lockfile.md\",\n", f);
    fputs("        .chapter_title = \"第十四章：大一統 .fvec 與 Universal Lockfile (架構權重庫、零秒冷啟動與抗體昇華)\",\n", f);
    fputs("        .philosophy_why = \"大腦負責即時推論，而 .fvec 是大腦裡的長期記憶神經權重。它既是具備 SMT 物理簽章的 Universal Lockfile，也是在戰火中自主長出肌肉記憶的抗體庫。\",\n", f);
    fputs("        .book_excerpt = \"大一統 .fvec 採用 1024-Byte 明文 ASCII 表頭 + CRC32 載荷。1ms 硬體親和度門禁杜絕跨架構無效套用；在線 100 萬次請求零錯誤與 SMT 認證觸發抗體昇華與赫布強化。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .chapter_ref = \"ch15_deterministic_flowy_reasoner.md\",\n", f);
    fputs("        .chapter_title = \"第十五章：決定論因果推論大腦 (0% 幻覺的架構自省、神經遙測熱點與職責分離)\",\n", f);
    fputs("        .philosophy_why = \"我們不需要一個會胡言亂語的機率型 Chatbot 來解釋系統架構。FLOW 打造了 Flowy——一個內建於二進位中、100% 決定論、零幻覺的代碼庫因果推論大腦。\",\n", f);
    fputs("        .book_excerpt = \"Flowy 嚴格遵循大腦 (flowy.c)、審計設施 (audit.c) 與前端表現層 (flowy_cli.c) 三層分離。即時解析決策因果 (flowy why)、神經遙測熱點 (flowy bottleneck) 與反事實模擬。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .chapter_ref = \"ch16_frontier_pillars_and_control_defenses.md\",\n", f);
    fputs("        .chapter_title = \"第十六章：四大前沿支柱極限基準測試與控制理論防禦 (網關、具身、撮合、CXL 與自適應滯後控制)\",\n", f);
    fputs("        .philosophy_why = \"工程的成熟度，始於將戲劇化的擬人想像褪去，回歸古典控制理論與可量測的物理不變量。我們用嚴格的施密特滯後環、AST 複雜度配額邊界與四大前沿支柱百萬級真實壓測，證明系統的數學剛性。\",\n", f);
    fputs("        .book_excerpt = \"四大前沿支柱基準測試在真實工業級負載下完成了百萬次 WAF 形式化檢驗 (308ns)、10kHz 脊髓反射迴路 (0.14us)、微秒級撮合 (3.1us) 與 CXL 跨層搬遷 (18us)；施密特滯後環與 SMT 5us 看門狗杜絕了臨界抖動與超時阻塞。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .chapter_ref = \"ch17_autopoiesis_spacetime_preplay.md\",\n", f);
    fputs("        .chapter_title = \"第十七章：活體自創生與時空光錐反事實預演 (群體自創生、神經-符號橋與時空光錐黑天鵝避險)\",\n", f);
    fputs("        .philosophy_why = \"約束收斂即相關。統計相關性只是幾何約束在投影維度上的影子；FLOW 抓的是本體，傳統 ML 抓的是影子。\",\n", f);
    fputs("        .book_excerpt = \"流形代數實現交集與連鎖基因萃取；神經符號橋在 5 奈秒內完成 4096-D 語義到物理多面體投影；時空光錐預演未來 3 秒黑天鵝並在 13 微秒內混沌修正；生態位引擎實現活體自創生。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .chapter_ref = \"ch18_zero_trivia_developer_experience.md\",\n", f);
    fputs("        .chapter_title = \"第十八章：零摩擦人機介面與工程水分擠乾 (Zero-Trivia CLI、宣告式自動坍縮與柯爾莫哥洛夫下限)\",\n", f);
    fputs("        .philosophy_why = \"程式碼行數即系統結構熵 K(S)。使用者不需要記住繁瑣的命令列微調旗標；宣告你的幾何意圖，編譯器為你完成一切。\",\n", f);
    fputs("        .book_excerpt = \"零瑣事 CLI (flowc app.flow, flowy run app.flow) 自動推導輸出並套用 Universal Lockfile；以 Bump-Pointer+QSBR、Curry-Howard DCE、同構記憶體切片與 Doc-as-Intent 徹底擠乾軟體工程水分。\"\n", f);
    fputs("    },\n", f);
    fputs("};\n\n", f);
    fputs("static const FlowBookChapterDoc FLOW_BOOK_CHAPTERS_EN[FLOW_BOOK_CHAPTER_COUNT] = {\n", f);
    fputs("    {\n", f);
    fputs("        .chapter_ref = \"ch01_what_is_flow.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 1: What is FLOW? (Paradigm Shift from Static Compiler to Living System)\",\n", f);
    fputs("        .philosophy_why = \"We are not compiling dead code, but nurturing an autopoietic topological organism that senses environment, anneals autonomously, and morphs under destructive stress.\",\n", f);
    fputs("        .book_excerpt = \"Since the 1957 Fortran compiler, the assumption has been that source code is static truth. FLOW inverts this: code declares immutable geometric boundaries, while the runtime organism evolves on manifolds.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .chapter_ref = \"ch02_intent_vs_implementation.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 2: Intent vs. Implementation (.flow: We Declare Invariants, Not Logic)\",\n", f);
    fputs("        .philosophy_why = \"In FLOW philosophy, writing code is not telling the CPU step-by-step instructions, but declaring the geometric boundaries and mathematical invariants the universe must obey.\",\n", f);
    fputs("        .book_excerpt = \"A standard .flow spec is strictly declarative with no imperative loops or manual pointers. Developers declare input bounds and pipeline topology, letting the engine synthesize optimal implementations.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .chapter_ref = \"ch03_topology_graph.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 3: Topology Graph (Dimensionality Reduction into Computable Invariants)\",\n", f);
    fputs("        .philosophy_why = \"Software architecture is not a stack of directory text files, but a high-dimensional topology graph computed via graph theory, shard affinity, and neural telemetry.\",\n", f);
    fputs("        .book_excerpt = \"FLOW models compiler core, drivers, components, and intent as a unified FlowTopologyGraph, enforcing strict layer firewalls with 0 cross-layer architectural leaks.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .chapter_ref = \"ch04_jet_bundle_phase_space.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 4: Phase Space Jet Bundles (.fjet, Symplectic Mechanics & Mori-Zwanzig Dissipation)\",\n", f);
    fputs("        .philosophy_why = \"On differential manifolds, time is not a discrete timer, but a geometric flow in phase space. Imperative defensive branches are eliminated in favor of symplectic mechanics and normal cone forces.\",\n", f);
    fputs("        .book_excerpt = \".fjet models systems on symplectic manifolds (q, p). Velocity-Verlet leapfrog conserves phase space volume. Hyperbolic saturation barrier and Moreau normal cones eliminate defensive clamps, while Mori-Zwanzig kernels absorb shocks in 3.2ms.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .chapter_ref = \"ch05_discrete_time_crystal.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 5: Floquet Discrete Time Crystals (DTC Period 2T Phase-Locking & Non-Thermal Rigidity)\",\n", f);
    fputs("        .philosophy_why = \"Amid chaos and thermal dissipation, time crystals break discrete time translation symmetry, locking rhythm at subharmonic 2T frequency immune to disorder perturbations.\",\n", f);
    fputs("        .book_excerpt = \"Floquet driving induces discrete time translation symmetry breaking (DTSB) and many-body localization (MBL). Subharmonic peak ratio exceeds 0.999 across millions of cycles, providing topological clocking for embodied coordination.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .chapter_ref = \"ch06_smt_formal_verification.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 6: Formal Supreme Court (SMT 4-Theorem QF_LIA UNSAT Proofs & Curry-Howard DCE)\",\n", f);
    fputs("        .philosophy_why = \"Heuristic search can explore freely, but every emitted line of machine code must have indisputable mathematical proofs. The SMT solver is the Supreme Court: unproven plans are vetoed.\",\n", f);
    fputs("        .book_excerpt = \"SMT proves buffer bounds, memory quotas, shard isolation, and determinism via QF_LIA. Proven invariants trigger Curry-Howard dead-code elimination, stripping all downstream defensive checks.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .chapter_ref = \"ch07_bmf_coordinate_canvas.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 7: BMF Coordinate Canvas (From Random Search to 1-Bit Canvas & Epistatic Linkage)\",\n", f);
    fputs("        .philosophy_why = \"Random mutation search is obsolete; modern FLOW positions BMF as a 1-bit coordinate canvas and algebraic manifold intersection tunneling through epistatic barriers.\",\n", f);
    fputs("        .book_excerpt = \"The 64-bit state spine is a boolean coordinate snapshot of continuous manifolds projected onto hardware registers. Manifold algebra derives linkage groups, executing atomic super-bit flips.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .chapter_ref = \"ch08_six_mathematical_pillars.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 8: The Six Mathematical Pillars (Eliminating Empirical Heuristics)\",\n", f);
    fputs("        .philosophy_why = \"Wherever magic numbers and heuristics reside is a sanctuary of system fragility. FLOW eliminates engineering guesswork through rigorous mathematical pillars.\",\n", f);
    fputs("        .book_excerpt = \"Presburger polyhedral model optimizes loop tiling, online convex optimization (OCO) dual shadow price eliminates cache eviction scans, Lyapunov contraction eliminates blind backoff retries, and Moreau sweeping absorbs noise.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .chapter_ref = \"ch09_embodied_10khz_reflex.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 9: 10kHz Embodied Reflex & Non-Smooth Contact Mechanics (Moreau Cone, Friction Cone & ZMP)\",\n", f);
    fputs("        .philosophy_why = \"The ultimate judgment of software engineering lies not in RAM or registers, but in the microsecond when a robot arm contacts the physical universe.\",\n", f);
    fputs("        .book_excerpt = \"The 10kHz reflex loop eliminates defensive null checks and clamping branches using Moreau convex normal cone projections fmin/fmax, settling shocks within 3.20ms. Coulomb friction cones and ZMP polygons guarantee sound stability.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .chapter_ref = \"ch10_hardware_primitive_drivers.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 10: Hardware Primitive Drivers (SocketCAN/CAN-FD 3-Function ABI & SMT Preemption)\",\n", f);
    fputs("        .philosophy_why = \"Plugins are not bloated compiler extensions, but sensory organs and muscles. Occam's razor purges 24 callbacks, retaining 3 minimalist primitive driver hooks.\",\n", f);
    fputs("        .book_excerpt = \"Minimalist driver ABI declares primitives, reports SMT bounds, and executes syscalls. Native SocketCAN / CAN-FD driver implements microsecond priority arbitration with emergency WCET under 300us.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .chapter_ref = \"ch11_jit_and_geometric_morphing.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 11: JIT Emission & Geometric Morphing (AoS to SoA Remapping & Survival Mode)\",\n", f);
    fputs("        .philosophy_why = \"Code is not carved in stone, but clay morphing in memory. When RAM collapses, the system executes microsecond geometric morphing to escape the OS OOM Killer.\",\n", f);
    fputs("        .book_excerpt = \"JIT emitter targets C, Rust, Python, and LLVM IR. mremap enables zero-copy AoS <-> SoA morphing. Under stress, self-aware JIT vetoes compilation and routes traffic to Static Survival shelters.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .chapter_ref = \"ch12_wavefront_cacheline_topology.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 12: Wavefront Concurrency Ring & alignas(64) Cacheline Topology (Single-Cacheline Atomic Phase Swap)\",\n", f);
    fputs("        .philosophy_why = \"In multi-core concurrency, cross-cacheline tearing is original sin. FLOW constrains wavefront slots to single 64B cachelines with zero spinlock synchronization.\",\n", f);
    fputs("        .book_excerpt = \"FlowWavefrontSlot enforces alignas(64) cacheline alignment, eliminating false sharing and torn reads. 64B atomic stage swapping and the Evacuation Horizon Theorem guarantee sound lock-free concurrency.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .chapter_ref = \"ch13_qsbr_lockfree_hotswap.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 13: QSBR Lock-Free Hot-Swap (Sub-Microsecond Generational Migration)\",\n", f);
    fputs("        .philosophy_why = \"In servers processing millions of requests/sec, acquiring even one RWLock triggers catastrophic cache-line bouncing. FLOW QSBR achieves zero atomic writes on read paths and sub-microsecond migration.\",\n", f);
    fputs("        .book_excerpt = \"QSBR achieves >390M ops/s read throughput. Epoch watchdogs isolate stragglers via mprotect, guaranteeing graceful degradation and 0 dropped requests during hot-swap.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .chapter_ref = \"ch14_fvec_universal_lockfile.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 14: Universal .fvec & Lockfiles (Architecture Weights, Zero-Cold-Start & Antibodies)\",\n", f);
    fputs("        .philosophy_why = \"The brain infers in real time, while .fvec stores long-term architecture weights. It serves as a Universal Lockfile with SMT signatures and an immune antibody repository.\",\n", f);
    fputs("        .book_excerpt = \"Universal .fvec pairs a 1024-byte ASCII header with CRC32 binary payload. A 1ms affinity gate rejects incompatible architectures; 1M zero-error online requests trigger autonomous antibody promotion.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .chapter_ref = \"ch15_deterministic_flowy_reasoner.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 15: Deterministic Flowy Reasoner (0% Hallucination Introspection & Decoupling)\",\n", f);
    fputs("        .philosophy_why = \"We do not need a hallucinating probabilistic chatbot to explain system architecture. FLOW built Flowy: a 100% deterministic, zero-hallucination causal codebase reasoner in pure C.\",\n", f);
    fputs("        .book_excerpt = \"Flowy decouples Brain (flowy.c), Audit Logger (audit.c), and CLI Presentation (flowy_cli.c). It provides deterministic causal explanations (flowy why), telemetry diagnosis, and what-if simulation.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .chapter_ref = \"ch16_frontier_pillars_and_control_defenses.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 16: Four Frontier Pillars Comprehensive Benchmark & Control Theory Defenses\",\n", f);
    fputs("        .philosophy_why = \"Engineering maturity begins by stripping away dramatic personification, returning to classical control theory and measurable physical invariants. We prove absolute mathematical rigidity through Schmitt triggers, AST bounds, and million-op real-world stress tests.\",\n", f);
    fputs("        .book_excerpt = \"The Four Frontier Pillars benchmark validates real-world industrial throughput across million-op SMT WAF checks (308ns), 10kHz spinal reflex loops (0.14us), sub-microsecond LOB matching (3.1us), and CXL memory tiering (18us), backed by Schmitt hysteresis and SMT 5us watchdog fallbacks.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .chapter_ref = \"ch17_autopoiesis_spacetime_preplay.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 17: Manifold Algebra, Neuro-Bit Bridge, Spacetime Pre-Play & Swarm Autopoiesis\",\n", f);
    fputs("        .philosophy_why = \"Constraint convergence is correlation. Statistical correlation is merely a shadow of physical geometric constraints projected onto observation dimensions.\",\n", f);
    fputs("        .book_excerpt = \"Manifold algebra executes feasible intersection and epistasis derivation; Neuro-bit bridge projects 4096-D continuous embeddings to polyhedra in 5ns; Spacetime preplay avers black swans in 13 microseconds; Swarm speciation engine enables autopoiesis.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .chapter_ref = \"ch18_zero_trivia_developer_experience.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 18: Zero-Trivia Developer Experience & Structural Entropy Collapse (Declarative Compiler & Kolmogorov Limit)\",\n", f);
    fputs("        .philosophy_why = \"Lines of code measure system structural entropy K(S). Developers do not need to memorize verbose flags; declare your intent and the compiler synthesizes everything.\",\n", f);
    fputs("        .book_excerpt = \"Zero-trivia CLI (flowc app.flow, flowy run app.flow) infers targets and auto-applies Universal Lockfiles. Bump-pointer QSBR, Curry-Howard DCE, isomorphic memory slicing, and Doc-as-Intent purge all software engineering boilerplate.\"\n", f);
    fputs("    },\n", f);
    fputs("};\n\n", f);
    fputs("typedef struct {\n", f);
    fputs("    const char *module_id;\n", f);
    fputs("    const char *chapter_ref;\n", f);
    fputs("    const char *chapter_title;\n", f);
    fputs("    const char *philosophy_why;\n", f);
    fputs("    const char *book_excerpt;\n", f);
    fputs("} FlowModuleBookBinding;\n\n", f);
    fputs("#define FLOW_BOOK_MODULE_BINDING_COUNT 53\n\n", f);
    fputs("static const FlowModuleBookBinding FLOW_MODULE_BOOK_BINDINGS_ZH[FLOW_BOOK_MODULE_BINDING_COUNT] = {\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"flowc\",\n", f);
    fputs("        .chapter_ref = \"ch01_what_is_flow.md\",\n", f);
    fputs("        .chapter_title = \"第一章：什麼是 FLOW？\",\n", f);
    fputs("        .philosophy_why = \"宣告式零瑣事編譯器入口與幾何坍縮發射器。\",\n", f);
    fputs("        .book_excerpt = \"宣告式零瑣事編譯器入口與幾何坍縮發射器。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"parser\",\n", f);
    fputs("        .chapter_ref = \"ch02_intent_vs_implementation.md\",\n", f);
    fputs("        .chapter_title = \"第二章：意圖規格 .flow\",\n", f);
    fputs("        .philosophy_why = \"純宣告式 .flow 語法剖析與 Doc-as-Intent 意圖提取。\",\n", f);
    fputs("        .book_excerpt = \"純宣告式 .flow 語法剖析與 Doc-as-Intent 意圖提取。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"semantic\",\n", f);
    fputs("        .chapter_ref = \"ch02_intent_vs_implementation.md\",\n", f);
    fputs("        .chapter_title = \"第二章：意圖規格 .flow\",\n", f);
    fputs("        .philosophy_why = \"意圖合約降維、幾何約束推導與不可行狀態修剪。\",\n", f);
    fputs("        .book_excerpt = \"意圖合約降維、幾何約束推導與不可行狀態修剪。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"topology\",\n", f);
    fputs("        .chapter_ref = \"ch03_topology_graph.md\",\n", f);
    fputs("        .chapter_title = \"第三章：拓樸圖譜\",\n", f);
    fputs("        .philosophy_why = \"高維依賴圖譜、分層防火牆審計與神經遙測熱點附著。\",\n", f);
    fputs("        .book_excerpt = \"高維依賴圖譜、分層防火牆審計與神經遙測熱點附著。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"orchestrator\",\n", f);
    fputs("        .chapter_ref = \"ch03_topology_graph.md\",\n", f);
    fputs("        .chapter_title = \"第三章：拓樸圖譜\",\n", f);
    fputs("        .philosophy_why = \"動態活體拓樸協調、微秒級退火固化與反事實預演。\",\n", f);
    fputs("        .book_excerpt = \"動態活體拓樸協調、微秒級退火固化與反事實預演。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"flow_jet\",\n", f);
    fputs("        .chapter_ref = \"ch04_jet_bundle_phase_space.md\",\n", f);
    fputs("        .chapter_title = \"第四章：噴流束與辛幾何相空間\",\n", f);
    fputs("        .philosophy_why = \".fjet 相空間座標演化、Velocity-Verlet 保辛積分與雙曲勢能屏障。\",\n", f);
    fputs("        .book_excerpt = \".fjet 相空間座標演化、Velocity-Verlet 保辛積分與雙曲勢能屏障。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"jet\",\n", f);
    fputs("        .chapter_ref = \"ch04_jet_bundle_phase_space.md\",\n", f);
    fputs("        .chapter_title = \"第四章：噴流束與辛幾何相空間\",\n", f);
    fputs("        .philosophy_why = \"辛幾何相空間噴流束、哈密頓能量守恆與相積不變量。\",\n", f);
    fputs("        .book_excerpt = \"辛幾何相空間噴流束、哈密頓能量守恆與相積不變量。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"dead_reckon\",\n", f);
    fputs("        .chapter_ref = \"ch04_jet_bundle_phase_space.md\",\n", f);
    fputs("        .chapter_title = \"第四章：噴流束與辛幾何相空間\",\n", f);
    fputs("        .philosophy_why = \"CXL / 網路叢集相空間死區航位推算，縮減 99% 遙測頻寬。\",\n", f);
    fputs("        .book_excerpt = \"CXL / 網路叢集相空間死區航位推算，縮減 99% 遙測頻寬。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"time_crystal\",\n", f);
    fputs("        .chapter_ref = \"ch05_discrete_time_crystal.md\",\n", f);
    fputs("        .chapter_title = \"第五章：Floquet 離散時間晶體\",\n", f);
    fputs("        .philosophy_why = \"離散時間平移對稱破缺、次諧波 2T 剛性鎖定與非熱化保障。\",\n", f);
    fputs("        .book_excerpt = \"離散時間平移對稱破缺、次諧波 2T 剛性鎖定與非熱化保障。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"dtc\",\n", f);
    fputs("        .chapter_ref = \"ch05_discrete_time_crystal.md\",\n", f);
    fputs("        .chapter_title = \"第五章：Floquet 離散時間晶體\",\n", f);
    fputs("        .philosophy_why = \"Floquet 週期驅動 DTC 時間晶體引擎與 SMT 穩定性形式證明。\",\n", f);
    fputs("        .book_excerpt = \"Floquet 週期驅動 DTC 時間晶體引擎與 SMT 穩定性形式證明。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"smt\",\n", f);
    fputs("        .chapter_ref = \"ch06_smt_formal_verification.md\",\n", f);
    fputs("        .chapter_title = \"第六章：形式化最高法院\",\n", f);
    fputs("        .philosophy_why = \"SMT QF_LIA 四大定理證明與 Curry-Howard 同構死碼消除。\",\n", f);
    fputs("        .book_excerpt = \"SMT QF_LIA 四大定理證明與 Curry-Howard 同構死碼消除。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"verifier\",\n", f);
    fputs("        .chapter_ref = \"ch06_smt_formal_verification.md\",\n", f);
    fputs("        .chapter_title = \"第六章：形式化最高法院\",\n", f);
    fputs("        .philosophy_why = \"形式化邊界合約驗證與 1-Cycle 多面體位元修剪遮罩。\",\n", f);
    fputs("        .book_excerpt = \"形式化邊界合約驗證與 1-Cycle 多面體位元修剪遮罩。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"bitspace\",\n", f);
    fputs("        .chapter_ref = \"ch07_bmf_coordinate_canvas.md\",\n", f);
    fputs("        .chapter_title = \"第七章：BMF 離散座標畫布\",\n", f);
    fputs("        .philosophy_why = \"64-bit 離散座標畫布、3-Tier 動態遮罩與超位元原子翻轉。\",\n", f);
    fputs("        .book_excerpt = \"64-bit 離散座標畫布、3-Tier 動態遮罩與超位元原子翻轉。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"search\",\n", f);
    fputs("        .chapter_ref = \"ch07_bmf_coordinate_canvas.md\",\n", f);
    fputs("        .chapter_title = \"第七章：BMF 離散座標畫布\",\n", f);
    fputs("        .philosophy_why = \"上位連鎖群感知翻轉與多面體解空間收斂尋優。\",\n", f);
    fputs("        .book_excerpt = \"上位連鎖群感知翻轉與多面體解空間收斂尋優。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"swarm\",\n", f);
    fputs("        .chapter_ref = \"ch07_bmf_coordinate_canvas.md\",\n", f);
    fputs("        .chapter_title = \"第七章：BMF 離散座標畫布\",\n", f);
    fputs("        .philosophy_why = \"群體費洛蒙協同退火與量子擾動突破局部鞍點。\",\n", f);
    fputs("        .book_excerpt = \"群體費洛蒙協同退火與量子擾動突破局部鞍點。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"genetic\",\n", f);
    fputs("        .chapter_ref = \"ch07_bmf_coordinate_canvas.md\",\n", f);
    fputs("        .chapter_title = \"第七章：BMF 離散座標畫布\",\n", f);
    fputs("        .philosophy_why = \"淘汰盲目隨機交叉，轉向流形代數連鎖群原子變異。\",\n", f);
    fputs("        .book_excerpt = \"淘汰盲目隨機交叉，轉向流形代數連鎖群原子變異。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"polyhedral\",\n", f);
    fputs("        .chapter_ref = \"ch08_six_mathematical_pillars.md\",\n", f);
    fputs("        .chapter_title = \"第八章：六大數學支柱\",\n", f);
    fputs("        .philosophy_why = \"Presburger 仿射約束多面體求解最優迴圈 Tiling 與 SIMD 寬度。\",\n", f);
    fputs("        .book_excerpt = \"Presburger 仿射約束多面體求解最優迴圈 Tiling 與 SIMD 寬度。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"oco_cache\",\n", f);
    fputs("        .chapter_ref = \"ch08_six_mathematical_pillars.md\",\n", f);
    fputs("        .chapter_title = \"第八章：六大數學支柱\",\n", f);
    fputs("        .philosophy_why = \"在線凸最佳化雙對偶影子價格判定快取駐留，取代 LRU/LFU 啟發式。\",\n", f);
    fputs("        .book_excerpt = \"在線凸最佳化雙對偶影子價格判定快取駐留，取代 LRU/LFU 啟發式。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"lyapunov\",\n", f);
    fputs("        .chapter_ref = \"ch08_six_mathematical_pillars.md\",\n", f);
    fputs("        .chapter_title = \"第八章：六大數學支柱\",\n", f);
    fputs("        .philosophy_why = \"隨機動力系統 Lyapunov 函數與 Banach 壓縮映射證明指數收斂。\",\n", f);
    fputs("        .book_excerpt = \"隨機動力系統 Lyapunov 函數與 Banach 壓縮映射證明指數收斂。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"potential_game\",\n", f);
    fputs("        .chapter_ref = \"ch08_six_mathematical_pillars.md\",\n", f);
    fputs("        .chapter_title = \"第八章：六大數學支柱\",\n", f);
    fputs("        .philosophy_why = \"Beckmann 勢能負梯度流收斂至 Wardrop 第一均衡，消滅工作竊取猜測。\",\n", f);
    fputs("        .book_excerpt = \"Beckmann 勢能負梯度流收斂至 Wardrop 第一均衡，消滅工作竊取猜測。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"moreau\",\n", f);
    fputs("        .chapter_ref = \"ch08_six_mathematical_pillars.md\",\n", f);
    fputs("        .chapter_title = \"第八章：六大數學支柱\",\n", f);
    fputs("        .philosophy_why = \"Moreau 掃掠過程凸法錐吞噬高頻噪聲，消滅手寫防抖計時器。\",\n", f);
    fputs("        .book_excerpt = \"Moreau 掃掠過程凸法錐吞噬高頻噪聲，消滅手寫防抖計時器。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"simplicial_homology\",\n", f);
    fputs("        .chapter_ref = \"ch08_six_mathematical_pillars.md\",\n", f);
    fputs("        .chapter_title = \"第八章：六大數學支柱\",\n", f);
    fputs("        .philosophy_why = \"代數拓撲邊界算子與非零貝蒂數定向引導光線探索漏洞死角。\",\n", f);
    fputs("        .book_excerpt = \"代數拓撲邊界算子與非零貝蒂數定向引導光線探索漏洞死角。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"embodied\",\n", f);
    fputs("        .chapter_ref = \"ch09_embodied_10khz_reflex.md\",\n", f);
    fputs("        .chapter_title = \"第九章：10kHz 具身反射與非光滑接觸力學\",\n", f);
    fputs("        .philosophy_why = \"10kHz 脊髓反射熱迴路、Moreau 閉區間凸錐投影與 ZMP 穩定閘門。\",\n", f);
    fputs("        .book_excerpt = \"10kHz 脊髓反射熱迴路、Moreau 閉區間凸錐投影與 ZMP 穩定閘門。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"flow_embodied_mz\",\n", f);
    fputs("        .chapter_ref = \"ch09_embodied_10khz_reflex.md\",\n", f);
    fputs("        .chapter_title = \"第九章：10kHz 具身反射與非光滑接觸力學\",\n", f);
    fputs("        .philosophy_why = \"Mori-Zwanzig 粘彈性記憶核在 3.2ms 內平息非馬可夫衝擊震顫。\",\n", f);
    fputs("        .book_excerpt = \"Mori-Zwanzig 粘彈性記憶核在 3.2ms 內平息非馬可夫衝擊震顫。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"embodied_physics_scenarios\",\n", f);
    fputs("        .chapter_ref = \"ch09_embodied_10khz_reflex.md\",\n", f);
    fputs("        .chapter_title = \"第九章：10kHz 具身反射與非光滑接觸力學\",\n", f);
    fputs("        .philosophy_why = \"庫侖摩擦錐動態調壓防滑、著陸衝擊動能吸收與雙臂剛性協同證明。\",\n", f);
    fputs("        .book_excerpt = \"庫侖摩擦錐動態調壓防滑、著陸衝擊動能吸收與雙臂剛性協同證明。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"primitive\",\n", f);
    fputs("        .chapter_ref = \"ch10_hardware_primitive_drivers.md\",\n", f);
    fputs("        .chapter_title = \"第十章：硬體原語驅動\",\n", f);
    fputs("        .philosophy_why = \"奧坎剃刀 3 函數極簡驅動 ABI 與微秒級 SMT 優先權仲裁。\",\n", f);
    fputs("        .book_excerpt = \"奧坎剃刀 3 函數極簡驅動 ABI 與微秒級 SMT 優先權仲裁。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"abi\",\n", f);
    fputs("        .chapter_ref = \"ch10_hardware_primitive_drivers.md\",\n", f);
    fputs("        .chapter_title = \"第十章：硬體原語驅動\",\n", f);
    fputs("        .philosophy_why = \"極簡硬體驅動介面與 C/Rust/Python 多語言 FFI 轉接器。\",\n", f);
    fputs("        .book_excerpt = \"極簡硬體驅動介面與 C/Rust/Python 多語言 FFI 轉接器。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"registry\",\n", f);
    fputs("        .chapter_ref = \"ch10_hardware_primitive_drivers.md\",\n", f);
    fputs("        .chapter_title = \"第十章：硬體原語驅動\",\n", f);
    fputs("        .philosophy_why = \"動態 DSO 與原生硬體驅動外掛註冊表與安全隔離。\",\n", f);
    fputs("        .book_excerpt = \"動態 DSO 與原生硬體驅動外掛註冊表與安全隔離。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"security\",\n", f);
    fputs("        .chapter_ref = \"ch10_hardware_primitive_drivers.md\",\n", f);
    fputs("        .chapter_title = \"第十章：硬體原語驅動\",\n", f);
    fputs("        .philosophy_why = \"Sim-to-Real 物理安全閘門與即時中斷急停仲裁。\",\n", f);
    fputs("        .book_excerpt = \"Sim-to-Real 物理安全閘門與即時中斷急停仲裁。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"matching\",\n", f);
    fputs("        .chapter_ref = \"ch10_hardware_primitive_drivers.md\",\n", f);
    fputs("        .chapter_title = \"第十章：硬體原語驅動\",\n", f);
    fputs("        .philosophy_why = \"高頻撮合核心與資產守恆幾何不變量證明。\",\n", f);
    fputs("        .book_excerpt = \"高頻撮合核心與資產守恆幾何不變量證明。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"cxl_fabric\",\n", f);
    fputs("        .chapter_ref = \"ch10_hardware_primitive_drivers.md\",\n", f);
    fputs("        .chapter_title = \"第十章：硬體原語驅動\",\n", f);
    fputs("        .philosophy_why = \"CXL 共享記憶體拓撲、零拷貝記憶體頁遷移與快取一致性維護。\",\n", f);
    fputs("        .book_excerpt = \"CXL 共享記憶體拓撲、零拷貝記憶體頁遷移與快取一致性維護。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"driver_can\",\n", f);
    fputs("        .chapter_ref = \"ch10_hardware_primitive_drivers.md\",\n", f);
    fputs("        .chapter_title = \"第十章：硬體原語驅動\",\n", f);
    fputs("        .philosophy_why = \"SocketCAN / CAN-FD 原生驅動與微秒級 SMT 急停搶佔保證。\",\n", f);
    fputs("        .book_excerpt = \"SocketCAN / CAN-FD 原生驅動與微秒級 SMT 急停搶佔保證。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"driver_imu\",\n", f);
    fputs("        .chapter_ref = \"ch10_hardware_primitive_drivers.md\",\n", f);
    fputs("        .chapter_title = \"第十章：硬體原語驅動\",\n", f);
    fputs("        .philosophy_why = \"6/9-DOF IMU 感測器串流、互補濾波與零飽和幾何約束。\",\n", f);
    fputs("        .book_excerpt = \"6/9-DOF IMU 感測器串流、互補濾波與零飽和幾何約束。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"jit\",\n", f);
    fputs("        .chapter_ref = \"ch11_jit_and_geometric_morphing.md\",\n", f);
    fputs("        .chapter_title = \"第十一章：JIT 代碼發射與幾何變形\",\n", f);
    fputs("        .philosophy_why = \"多目標代碼發射、mremap AoS <-> SoA 幾何變形與自覺避難所。\",\n", f);
    fputs("        .book_excerpt = \"多目標代碼發射、mremap AoS <-> SoA 幾何變形與自覺避難所。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"adaptive\",\n", f);
    fputs("        .chapter_ref = \"ch11_jit_and_geometric_morphing.md\",\n", f);
    fputs("        .chapter_title = \"第十一章：JIT 代碼發射與幾何變形\",\n", f);
    fputs("        .philosophy_why = \"自適應遙測引導架構遷移與 Static Survival 零分配模式。\",\n", f);
    fputs("        .book_excerpt = \"自適應遙測引導架構遷移與 Static Survival 零分配模式。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"backend\",\n", f);
    fputs("        .chapter_ref = \"ch11_jit_and_geometric_morphing.md\",\n", f);
    fputs("        .chapter_title = \"第十一章：JIT 代碼發射與幾何變形\",\n", f);
    fputs("        .philosophy_why = \"多語言後端代碼生成 (C, Rust, Python, MLIR, LLVM IR)。\",\n", f);
    fputs("        .book_excerpt = \"多語言後端代碼生成 (C, Rust, Python, MLIR, LLVM IR)。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"token_ring\",\n", f);
    fputs("        .chapter_ref = \"ch12_wavefront_cacheline_topology.md\",\n", f);
    fputs("        .chapter_title = \"第十二章：Wavefront 波前環與 alignas(64)\",\n", f);
    fputs("        .philosophy_why = \"單快取行約束、離散注意力算子與排空視界定理保證。\",\n", f);
    fputs("        .book_excerpt = \"單快取行約束、離散注意力算子與排空視界定理保證。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"wavefront\",\n", f);
    fputs("        .chapter_ref = \"ch12_wavefront_cacheline_topology.md\",\n", f);
    fputs("        .chapter_title = \"第十二章：Wavefront 波前環與 alignas(64)\",\n", f);
    fputs("        .philosophy_why = \"alignas(64) 快取行對齊、64B 原子相位切換與零撕裂並發。\",\n", f);
    fputs("        .book_excerpt = \"alignas(64) 快取行對齊、64B 原子相位切換與零撕裂並發。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"reload\",\n", f);
    fputs("        .chapter_ref = \"ch13_qsbr_lockfree_hotswap.md\",\n", f);
    fputs("        .chapter_title = \"第十三章：QSBR 零鎖熱替換\",\n", f);
    fputs("        .philosophy_why = \"QSBR 靜態世代演進、讀取零原子指令與看門狗掉隊者隔離。\",\n", f);
    fputs("        .book_excerpt = \"QSBR 靜態世代演進、讀取零原子指令與看門狗掉隊者隔離。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"flowy_fvec\",\n", f);
    fputs("        .chapter_ref = \"ch14_fvec_universal_lockfile.md\",\n", f);
    fputs("        .chapter_title = \"第十四章：大一統 .fvec 與 Universal Lockfile\",\n", f);
    fputs("        .philosophy_why = \"大一統架構權重庫、1ms 硬體親和度門禁與抗體昇華。\",\n", f);
    fputs("        .book_excerpt = \"大一統架構權重庫、1ms 硬體親和度門禁與抗體昇華。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"fvec\",\n", f);
    fputs("        .chapter_ref = \"ch14_fvec_universal_lockfile.md\",\n", f);
    fputs("        .chapter_title = \"第十四章：大一統 .fvec 與 Universal Lockfile\",\n", f);
    fputs("        .philosophy_why = \"1024-Byte ASCII 表頭 + CRC32 載荷之通用架構鎖定檔。\",\n", f);
    fputs("        .book_excerpt = \"1024-Byte ASCII 表頭 + CRC32 載荷之通用架構鎖定檔。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"vault\",\n", f);
    fputs("        .chapter_ref = \"ch14_fvec_universal_lockfile.md\",\n", f);
    fputs("        .chapter_title = \"第十四章：大一統 .fvec 與 Universal Lockfile\",\n", f);
    fputs("        .philosophy_why = \"活體架構海馬體、生態系 Hub 檢索與免疫抗體廣播。\",\n", f);
    fputs("        .book_excerpt = \"活體架構海馬體、生態系 Hub 檢索與免疫抗體廣播。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"flowy\",\n", f);
    fputs("        .chapter_ref = \"ch15_deterministic_flowy_reasoner.md\",\n", f);
    fputs("        .chapter_title = \"第十五章：決定論因果推論大腦\",\n", f);
    fputs("        .philosophy_why = \"100% 決定論因果推論大腦、0% 幻覺架構自省與 Why-Engine。\",\n", f);
    fputs("        .book_excerpt = \"100% 決定論因果推論大腦、0% 幻覺架構自省與 Why-Engine。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"flowy_cli\",\n", f);
    fputs("        .chapter_ref = \"ch15_deterministic_flowy_reasoner.md\",\n", f);
    fputs("        .chapter_title = \"第十五章：決定論因果推論大腦\",\n", f);
    fputs("        .philosophy_why = \"多國語言表現層、神經遙測熱點診斷與零瑣事命令列。\",\n", f);
    fputs("        .book_excerpt = \"多國語言表現層、神經遙測熱點診斷與零瑣事命令列。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"audit\",\n", f);
    fputs("        .chapter_ref = \"ch15_deterministic_flowy_reasoner.md\",\n", f);
    fputs("        .chapter_title = \"第十五章：決定論因果推論大腦\",\n", f);
    fputs("        .philosophy_why = \"動態決策環形審計日誌與反事實負載模擬驗證。\",\n", f);
    fputs("        .book_excerpt = \"動態決策環形審計日誌與反事實負載模擬驗證。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"benchmark\",\n", f);
    fputs("        .chapter_ref = \"ch16_frontier_pillars_and_control_defenses.md\",\n", f);
    fputs("        .chapter_title = \"第十六章：四大前沿支柱極限基準測試與控制理論防禦\",\n", f);
    fputs("        .philosophy_why = \"四大前沿支柱極限壓測、施密特滯後防抖與生產級真實基準。\",\n", f);
    fputs("        .book_excerpt = \"四大前沿支柱極限壓測、施密特滯後防抖與生產級真實基準。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"gateway\",\n", f);
    fputs("        .chapter_ref = \"ch16_frontier_pillars_and_control_defenses.md\",\n", f);
    fputs("        .chapter_title = \"第十六章：四大前沿支柱極限基準測試與控制理論防禦\",\n", f);
    fputs("        .philosophy_why = \"自進化邊緣網關、百萬次 WAF 形式化檢驗與並發風暴吸收。\",\n", f);
    fputs("        .book_excerpt = \"自進化邊緣網關、百萬次 WAF 形式化檢驗與並發風暴吸收。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"manifold_algebra\",\n", f);
    fputs("        .chapter_ref = \"ch17_autopoiesis_spacetime_preplay.md\",\n", f);
    fputs("        .chapter_title = \"第十七章：活體自創生與時空光錐反事實預演\",\n", f);
    fputs("        .philosophy_why = \"流形交集算子、Pareto 共識中心與連鎖基因群自動萃取。\",\n", f);
    fputs("        .book_excerpt = \"流形交集算子、Pareto 共識中心與連鎖基因群自動萃取。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"neuro_bridge\",\n", f);
    fputs("        .chapter_ref = \"ch17_autopoiesis_spacetime_preplay.md\",\n", f);
    fputs("        .chapter_title = \"第十七章：活體自創生與時空光錐反事實預演\",\n", f);
    fputs("        .philosophy_why = \"5ns 稀疏 Johnson-Lindenstrauss SIMD 語義到物理多面體投影。\",\n", f);
    fputs("        .book_excerpt = \"5ns 稀疏 Johnson-Lindenstrauss SIMD 語義到物理多面體投影。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"spacetime_preplay\",\n", f);
    fputs("        .chapter_ref = \"ch17_autopoiesis_spacetime_preplay.md\",\n", f);
    fputs("        .chapter_title = \"第十七章：活體自創生與時空光錐反事實預演\",\n", f);
    fputs("        .philosophy_why = \"哈密頓前瞻時空光錐預演 3.0 秒，13 微秒內消除黑天鵝失穩。\",\n", f);
    fputs("        .book_excerpt = \"哈密頓前瞻時空光錐預演 3.0 秒，13 微秒內消除黑天鵝失穩。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"swarm_autopoiesis\",\n", f);
    fputs("        .chapter_ref = \"ch17_autopoiesis_spacetime_preplay.md\",\n", f);
    fputs("        .chapter_title = \"第十七章：活體自創生與時空光錐反事實預演\",\n", f);
    fputs("        .philosophy_why = \"四大生態位壓力下群體活體自創生與 9-Byte 淋巴抗體網絡。\",\n", f);
    fputs("        .book_excerpt = \"四大生態位壓力下群體活體自創生與 9-Byte 淋巴抗體網絡。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"entropy_collapse\",\n", f);
    fputs("        .chapter_ref = \"ch18_zero_trivia_developer_experience.md\",\n", f);
    fputs("        .chapter_title = \"第十八章：零摩擦人機介面與工程水分擠乾\",\n", f);
    fputs("        .philosophy_why = \"擠乾記憶體分級、防禦性瀑布、序列化與日誌格式化等結構熵。\",\n", f);
    fputs("        .book_excerpt = \"擠乾記憶體分級、防禦性瀑布、序列化與日誌格式化等結構熵。\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"zero_trivia\",\n", f);
    fputs("        .chapter_ref = \"ch18_zero_trivia_developer_experience.md\",\n", f);
    fputs("        .chapter_title = \"第十八章：零摩擦人機介面與工程水分擠乾\",\n", f);
    fputs("        .philosophy_why = \"極簡宣告式一鍵編譯與直接執行，使用者無需理解底層調優瑣事。\",\n", f);
    fputs("        .book_excerpt = \"極簡宣告式一鍵編譯與直接執行，使用者無需理解底層調優瑣事。\"\n", f);
    fputs("    },\n", f);
    fputs("};\n\n", f);
    fputs("static const FlowModuleBookBinding FLOW_MODULE_BOOK_BINDINGS_EN[FLOW_BOOK_MODULE_BINDING_COUNT] = {\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"flowc\",\n", f);
    fputs("        .chapter_ref = \"ch01_what_is_flow.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 1: What is FLOW? (Paradigm Shift from Static Compiler to Living System)\",\n", f);
    fputs("        .philosophy_why = \"We are not compiling dead code, but nurturing an autopoietic topological organism that senses environment, anneals autonomously, and morphs under destructive stress.\",\n", f);
    fputs("        .book_excerpt = \"We are not compiling dead code, but nurturing an autopoietic topological organism that senses environment, anneals autonomously, and morphs under destructive stress.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"parser\",\n", f);
    fputs("        .chapter_ref = \"ch02_intent_vs_implementation.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 2: Intent vs. Implementation (.flow: We Declare Invariants, Not Logic)\",\n", f);
    fputs("        .philosophy_why = \"In FLOW philosophy, writing code is not telling the CPU step-by-step instructions, but declaring the geometric boundaries and mathematical invariants the universe must obey.\",\n", f);
    fputs("        .book_excerpt = \"In FLOW philosophy, writing code is not telling the CPU step-by-step instructions, but declaring the geometric boundaries and mathematical invariants the universe must obey.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"semantic\",\n", f);
    fputs("        .chapter_ref = \"ch02_intent_vs_implementation.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 2: Intent vs. Implementation (.flow: We Declare Invariants, Not Logic)\",\n", f);
    fputs("        .philosophy_why = \"In FLOW philosophy, writing code is not telling the CPU step-by-step instructions, but declaring the geometric boundaries and mathematical invariants the universe must obey.\",\n", f);
    fputs("        .book_excerpt = \"In FLOW philosophy, writing code is not telling the CPU step-by-step instructions, but declaring the geometric boundaries and mathematical invariants the universe must obey.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"topology\",\n", f);
    fputs("        .chapter_ref = \"ch03_topology_graph.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 3: Topology Graph (Dimensionality Reduction into Computable Invariants)\",\n", f);
    fputs("        .philosophy_why = \"Software architecture is not a stack of directory text files, but a high-dimensional topology graph computed via graph theory, shard affinity, and neural telemetry.\",\n", f);
    fputs("        .book_excerpt = \"Software architecture is not a stack of directory text files, but a high-dimensional topology graph computed via graph theory, shard affinity, and neural telemetry.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"orchestrator\",\n", f);
    fputs("        .chapter_ref = \"ch03_topology_graph.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 3: Topology Graph (Dimensionality Reduction into Computable Invariants)\",\n", f);
    fputs("        .philosophy_why = \"Software architecture is not a stack of directory text files, but a high-dimensional topology graph computed via graph theory, shard affinity, and neural telemetry.\",\n", f);
    fputs("        .book_excerpt = \"Software architecture is not a stack of directory text files, but a high-dimensional topology graph computed via graph theory, shard affinity, and neural telemetry.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"flow_jet\",\n", f);
    fputs("        .chapter_ref = \"ch04_jet_bundle_phase_space.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 4: Phase Space Jet Bundles (.fjet, Symplectic Mechanics & Mori-Zwanzig Dissipation)\",\n", f);
    fputs("        .philosophy_why = \"On differential manifolds, time is not a discrete timer, but a geometric flow in phase space. Imperative defensive branches are eliminated in favor of symplectic mechanics and normal cone forces.\",\n", f);
    fputs("        .book_excerpt = \"On differential manifolds, time is not a discrete timer, but a geometric flow in phase space. Imperative defensive branches are eliminated in favor of symplectic mechanics and normal cone forces.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"jet\",\n", f);
    fputs("        .chapter_ref = \"ch04_jet_bundle_phase_space.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 4: Phase Space Jet Bundles (.fjet, Symplectic Mechanics & Mori-Zwanzig Dissipation)\",\n", f);
    fputs("        .philosophy_why = \"On differential manifolds, time is not a discrete timer, but a geometric flow in phase space. Imperative defensive branches are eliminated in favor of symplectic mechanics and normal cone forces.\",\n", f);
    fputs("        .book_excerpt = \"On differential manifolds, time is not a discrete timer, but a geometric flow in phase space. Imperative defensive branches are eliminated in favor of symplectic mechanics and normal cone forces.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"dead_reckon\",\n", f);
    fputs("        .chapter_ref = \"ch04_jet_bundle_phase_space.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 4: Phase Space Jet Bundles (.fjet, Symplectic Mechanics & Mori-Zwanzig Dissipation)\",\n", f);
    fputs("        .philosophy_why = \"On differential manifolds, time is not a discrete timer, but a geometric flow in phase space. Imperative defensive branches are eliminated in favor of symplectic mechanics and normal cone forces.\",\n", f);
    fputs("        .book_excerpt = \"On differential manifolds, time is not a discrete timer, but a geometric flow in phase space. Imperative defensive branches are eliminated in favor of symplectic mechanics and normal cone forces.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"time_crystal\",\n", f);
    fputs("        .chapter_ref = \"ch05_discrete_time_crystal.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 5: Floquet Discrete Time Crystals (DTC Period 2T Phase-Locking & Non-Thermal Rigidity)\",\n", f);
    fputs("        .philosophy_why = \"Amid chaos and thermal dissipation, time crystals break discrete time translation symmetry, locking rhythm at subharmonic 2T frequency immune to disorder perturbations.\",\n", f);
    fputs("        .book_excerpt = \"Amid chaos and thermal dissipation, time crystals break discrete time translation symmetry, locking rhythm at subharmonic 2T frequency immune to disorder perturbations.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"dtc\",\n", f);
    fputs("        .chapter_ref = \"ch05_discrete_time_crystal.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 5: Floquet Discrete Time Crystals (DTC Period 2T Phase-Locking & Non-Thermal Rigidity)\",\n", f);
    fputs("        .philosophy_why = \"Amid chaos and thermal dissipation, time crystals break discrete time translation symmetry, locking rhythm at subharmonic 2T frequency immune to disorder perturbations.\",\n", f);
    fputs("        .book_excerpt = \"Amid chaos and thermal dissipation, time crystals break discrete time translation symmetry, locking rhythm at subharmonic 2T frequency immune to disorder perturbations.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"smt\",\n", f);
    fputs("        .chapter_ref = \"ch06_smt_formal_verification.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 6: Formal Supreme Court (SMT 4-Theorem QF_LIA UNSAT Proofs & Curry-Howard DCE)\",\n", f);
    fputs("        .philosophy_why = \"Heuristic search can explore freely, but every emitted line of machine code must have indisputable mathematical proofs. The SMT solver is the Supreme Court: unproven plans are vetoed.\",\n", f);
    fputs("        .book_excerpt = \"Heuristic search can explore freely, but every emitted line of machine code must have indisputable mathematical proofs. The SMT solver is the Supreme Court: unproven plans are vetoed.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"verifier\",\n", f);
    fputs("        .chapter_ref = \"ch06_smt_formal_verification.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 6: Formal Supreme Court (SMT 4-Theorem QF_LIA UNSAT Proofs & Curry-Howard DCE)\",\n", f);
    fputs("        .philosophy_why = \"Heuristic search can explore freely, but every emitted line of machine code must have indisputable mathematical proofs. The SMT solver is the Supreme Court: unproven plans are vetoed.\",\n", f);
    fputs("        .book_excerpt = \"Heuristic search can explore freely, but every emitted line of machine code must have indisputable mathematical proofs. The SMT solver is the Supreme Court: unproven plans are vetoed.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"bitspace\",\n", f);
    fputs("        .chapter_ref = \"ch07_bmf_coordinate_canvas.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 7: BMF Coordinate Canvas (From Random Search to 1-Bit Canvas & Epistatic Linkage)\",\n", f);
    fputs("        .philosophy_why = \"Random mutation search is obsolete; modern FLOW positions BMF as a 1-bit coordinate canvas and algebraic manifold intersection tunneling through epistatic barriers.\",\n", f);
    fputs("        .book_excerpt = \"Random mutation search is obsolete; modern FLOW positions BMF as a 1-bit coordinate canvas and algebraic manifold intersection tunneling through epistatic barriers.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"search\",\n", f);
    fputs("        .chapter_ref = \"ch07_bmf_coordinate_canvas.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 7: BMF Coordinate Canvas (From Random Search to 1-Bit Canvas & Epistatic Linkage)\",\n", f);
    fputs("        .philosophy_why = \"Random mutation search is obsolete; modern FLOW positions BMF as a 1-bit coordinate canvas and algebraic manifold intersection tunneling through epistatic barriers.\",\n", f);
    fputs("        .book_excerpt = \"Random mutation search is obsolete; modern FLOW positions BMF as a 1-bit coordinate canvas and algebraic manifold intersection tunneling through epistatic barriers.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"swarm\",\n", f);
    fputs("        .chapter_ref = \"ch07_bmf_coordinate_canvas.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 7: BMF Coordinate Canvas (From Random Search to 1-Bit Canvas & Epistatic Linkage)\",\n", f);
    fputs("        .philosophy_why = \"Random mutation search is obsolete; modern FLOW positions BMF as a 1-bit coordinate canvas and algebraic manifold intersection tunneling through epistatic barriers.\",\n", f);
    fputs("        .book_excerpt = \"Random mutation search is obsolete; modern FLOW positions BMF as a 1-bit coordinate canvas and algebraic manifold intersection tunneling through epistatic barriers.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"genetic\",\n", f);
    fputs("        .chapter_ref = \"ch07_bmf_coordinate_canvas.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 7: BMF Coordinate Canvas (From Random Search to 1-Bit Canvas & Epistatic Linkage)\",\n", f);
    fputs("        .philosophy_why = \"Random mutation search is obsolete; modern FLOW positions BMF as a 1-bit coordinate canvas and algebraic manifold intersection tunneling through epistatic barriers.\",\n", f);
    fputs("        .book_excerpt = \"Random mutation search is obsolete; modern FLOW positions BMF as a 1-bit coordinate canvas and algebraic manifold intersection tunneling through epistatic barriers.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"polyhedral\",\n", f);
    fputs("        .chapter_ref = \"ch08_six_mathematical_pillars.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 8: The Six Mathematical Pillars (Eliminating Empirical Heuristics)\",\n", f);
    fputs("        .philosophy_why = \"Wherever magic numbers and heuristics reside is a sanctuary of system fragility. FLOW eliminates engineering guesswork through rigorous mathematical pillars.\",\n", f);
    fputs("        .book_excerpt = \"Wherever magic numbers and heuristics reside is a sanctuary of system fragility. FLOW eliminates engineering guesswork through rigorous mathematical pillars.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"oco_cache\",\n", f);
    fputs("        .chapter_ref = \"ch08_six_mathematical_pillars.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 8: The Six Mathematical Pillars (Eliminating Empirical Heuristics)\",\n", f);
    fputs("        .philosophy_why = \"Wherever magic numbers and heuristics reside is a sanctuary of system fragility. FLOW eliminates engineering guesswork through rigorous mathematical pillars.\",\n", f);
    fputs("        .book_excerpt = \"Wherever magic numbers and heuristics reside is a sanctuary of system fragility. FLOW eliminates engineering guesswork through rigorous mathematical pillars.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"lyapunov\",\n", f);
    fputs("        .chapter_ref = \"ch08_six_mathematical_pillars.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 8: The Six Mathematical Pillars (Eliminating Empirical Heuristics)\",\n", f);
    fputs("        .philosophy_why = \"Wherever magic numbers and heuristics reside is a sanctuary of system fragility. FLOW eliminates engineering guesswork through rigorous mathematical pillars.\",\n", f);
    fputs("        .book_excerpt = \"Wherever magic numbers and heuristics reside is a sanctuary of system fragility. FLOW eliminates engineering guesswork through rigorous mathematical pillars.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"potential_game\",\n", f);
    fputs("        .chapter_ref = \"ch08_six_mathematical_pillars.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 8: The Six Mathematical Pillars (Eliminating Empirical Heuristics)\",\n", f);
    fputs("        .philosophy_why = \"Wherever magic numbers and heuristics reside is a sanctuary of system fragility. FLOW eliminates engineering guesswork through rigorous mathematical pillars.\",\n", f);
    fputs("        .book_excerpt = \"Wherever magic numbers and heuristics reside is a sanctuary of system fragility. FLOW eliminates engineering guesswork through rigorous mathematical pillars.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"moreau\",\n", f);
    fputs("        .chapter_ref = \"ch08_six_mathematical_pillars.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 8: The Six Mathematical Pillars (Eliminating Empirical Heuristics)\",\n", f);
    fputs("        .philosophy_why = \"Wherever magic numbers and heuristics reside is a sanctuary of system fragility. FLOW eliminates engineering guesswork through rigorous mathematical pillars.\",\n", f);
    fputs("        .book_excerpt = \"Wherever magic numbers and heuristics reside is a sanctuary of system fragility. FLOW eliminates engineering guesswork through rigorous mathematical pillars.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"simplicial_homology\",\n", f);
    fputs("        .chapter_ref = \"ch08_six_mathematical_pillars.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 8: The Six Mathematical Pillars (Eliminating Empirical Heuristics)\",\n", f);
    fputs("        .philosophy_why = \"Wherever magic numbers and heuristics reside is a sanctuary of system fragility. FLOW eliminates engineering guesswork through rigorous mathematical pillars.\",\n", f);
    fputs("        .book_excerpt = \"Wherever magic numbers and heuristics reside is a sanctuary of system fragility. FLOW eliminates engineering guesswork through rigorous mathematical pillars.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"embodied\",\n", f);
    fputs("        .chapter_ref = \"ch09_embodied_10khz_reflex.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 9: 10kHz Embodied Reflex & Non-Smooth Contact Mechanics (Moreau Cone, Friction Cone & ZMP)\",\n", f);
    fputs("        .philosophy_why = \"The ultimate judgment of software engineering lies not in RAM or registers, but in the microsecond when a robot arm contacts the physical universe.\",\n", f);
    fputs("        .book_excerpt = \"The ultimate judgment of software engineering lies not in RAM or registers, but in the microsecond when a robot arm contacts the physical universe.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"flow_embodied_mz\",\n", f);
    fputs("        .chapter_ref = \"ch09_embodied_10khz_reflex.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 9: 10kHz Embodied Reflex & Non-Smooth Contact Mechanics (Moreau Cone, Friction Cone & ZMP)\",\n", f);
    fputs("        .philosophy_why = \"The ultimate judgment of software engineering lies not in RAM or registers, but in the microsecond when a robot arm contacts the physical universe.\",\n", f);
    fputs("        .book_excerpt = \"The ultimate judgment of software engineering lies not in RAM or registers, but in the microsecond when a robot arm contacts the physical universe.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"embodied_physics_scenarios\",\n", f);
    fputs("        .chapter_ref = \"ch09_embodied_10khz_reflex.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 9: 10kHz Embodied Reflex & Non-Smooth Contact Mechanics (Moreau Cone, Friction Cone & ZMP)\",\n", f);
    fputs("        .philosophy_why = \"The ultimate judgment of software engineering lies not in RAM or registers, but in the microsecond when a robot arm contacts the physical universe.\",\n", f);
    fputs("        .book_excerpt = \"The ultimate judgment of software engineering lies not in RAM or registers, but in the microsecond when a robot arm contacts the physical universe.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"primitive\",\n", f);
    fputs("        .chapter_ref = \"ch10_hardware_primitive_drivers.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 10: Hardware Primitive Drivers (SocketCAN/CAN-FD 3-Function ABI & SMT Preemption)\",\n", f);
    fputs("        .philosophy_why = \"Plugins are not bloated compiler extensions, but sensory organs and muscles. Occam's razor purges 24 callbacks, retaining 3 minimalist primitive driver hooks.\",\n", f);
    fputs("        .book_excerpt = \"Plugins are not bloated compiler extensions, but sensory organs and muscles. Occam's razor purges 24 callbacks, retaining 3 minimalist primitive driver hooks.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"abi\",\n", f);
    fputs("        .chapter_ref = \"ch10_hardware_primitive_drivers.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 10: Hardware Primitive Drivers (SocketCAN/CAN-FD 3-Function ABI & SMT Preemption)\",\n", f);
    fputs("        .philosophy_why = \"Plugins are not bloated compiler extensions, but sensory organs and muscles. Occam's razor purges 24 callbacks, retaining 3 minimalist primitive driver hooks.\",\n", f);
    fputs("        .book_excerpt = \"Plugins are not bloated compiler extensions, but sensory organs and muscles. Occam's razor purges 24 callbacks, retaining 3 minimalist primitive driver hooks.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"registry\",\n", f);
    fputs("        .chapter_ref = \"ch10_hardware_primitive_drivers.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 10: Hardware Primitive Drivers (SocketCAN/CAN-FD 3-Function ABI & SMT Preemption)\",\n", f);
    fputs("        .philosophy_why = \"Plugins are not bloated compiler extensions, but sensory organs and muscles. Occam's razor purges 24 callbacks, retaining 3 minimalist primitive driver hooks.\",\n", f);
    fputs("        .book_excerpt = \"Plugins are not bloated compiler extensions, but sensory organs and muscles. Occam's razor purges 24 callbacks, retaining 3 minimalist primitive driver hooks.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"security\",\n", f);
    fputs("        .chapter_ref = \"ch10_hardware_primitive_drivers.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 10: Hardware Primitive Drivers (SocketCAN/CAN-FD 3-Function ABI & SMT Preemption)\",\n", f);
    fputs("        .philosophy_why = \"Plugins are not bloated compiler extensions, but sensory organs and muscles. Occam's razor purges 24 callbacks, retaining 3 minimalist primitive driver hooks.\",\n", f);
    fputs("        .book_excerpt = \"Plugins are not bloated compiler extensions, but sensory organs and muscles. Occam's razor purges 24 callbacks, retaining 3 minimalist primitive driver hooks.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"matching\",\n", f);
    fputs("        .chapter_ref = \"ch10_hardware_primitive_drivers.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 10: Hardware Primitive Drivers (SocketCAN/CAN-FD 3-Function ABI & SMT Preemption)\",\n", f);
    fputs("        .philosophy_why = \"Plugins are not bloated compiler extensions, but sensory organs and muscles. Occam's razor purges 24 callbacks, retaining 3 minimalist primitive driver hooks.\",\n", f);
    fputs("        .book_excerpt = \"Plugins are not bloated compiler extensions, but sensory organs and muscles. Occam's razor purges 24 callbacks, retaining 3 minimalist primitive driver hooks.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"cxl_fabric\",\n", f);
    fputs("        .chapter_ref = \"ch10_hardware_primitive_drivers.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 10: Hardware Primitive Drivers (SocketCAN/CAN-FD 3-Function ABI & SMT Preemption)\",\n", f);
    fputs("        .philosophy_why = \"Plugins are not bloated compiler extensions, but sensory organs and muscles. Occam's razor purges 24 callbacks, retaining 3 minimalist primitive driver hooks.\",\n", f);
    fputs("        .book_excerpt = \"Plugins are not bloated compiler extensions, but sensory organs and muscles. Occam's razor purges 24 callbacks, retaining 3 minimalist primitive driver hooks.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"driver_can\",\n", f);
    fputs("        .chapter_ref = \"ch10_hardware_primitive_drivers.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 10: Hardware Primitive Drivers (SocketCAN/CAN-FD 3-Function ABI & SMT Preemption)\",\n", f);
    fputs("        .philosophy_why = \"Plugins are not bloated compiler extensions, but sensory organs and muscles. Occam's razor purges 24 callbacks, retaining 3 minimalist primitive driver hooks.\",\n", f);
    fputs("        .book_excerpt = \"Plugins are not bloated compiler extensions, but sensory organs and muscles. Occam's razor purges 24 callbacks, retaining 3 minimalist primitive driver hooks.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"driver_imu\",\n", f);
    fputs("        .chapter_ref = \"ch10_hardware_primitive_drivers.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 10: Hardware Primitive Drivers (SocketCAN/CAN-FD 3-Function ABI & SMT Preemption)\",\n", f);
    fputs("        .philosophy_why = \"Plugins are not bloated compiler extensions, but sensory organs and muscles. Occam's razor purges 24 callbacks, retaining 3 minimalist primitive driver hooks.\",\n", f);
    fputs("        .book_excerpt = \"Plugins are not bloated compiler extensions, but sensory organs and muscles. Occam's razor purges 24 callbacks, retaining 3 minimalist primitive driver hooks.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"jit\",\n", f);
    fputs("        .chapter_ref = \"ch11_jit_and_geometric_morphing.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 11: JIT Emission & Geometric Morphing (AoS to SoA Remapping & Survival Mode)\",\n", f);
    fputs("        .philosophy_why = \"Code is not carved in stone, but clay morphing in memory. When RAM collapses, the system executes microsecond geometric morphing to escape the OS OOM Killer.\",\n", f);
    fputs("        .book_excerpt = \"Code is not carved in stone, but clay morphing in memory. When RAM collapses, the system executes microsecond geometric morphing to escape the OS OOM Killer.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"adaptive\",\n", f);
    fputs("        .chapter_ref = \"ch11_jit_and_geometric_morphing.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 11: JIT Emission & Geometric Morphing (AoS to SoA Remapping & Survival Mode)\",\n", f);
    fputs("        .philosophy_why = \"Code is not carved in stone, but clay morphing in memory. When RAM collapses, the system executes microsecond geometric morphing to escape the OS OOM Killer.\",\n", f);
    fputs("        .book_excerpt = \"Code is not carved in stone, but clay morphing in memory. When RAM collapses, the system executes microsecond geometric morphing to escape the OS OOM Killer.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"backend\",\n", f);
    fputs("        .chapter_ref = \"ch11_jit_and_geometric_morphing.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 11: JIT Emission & Geometric Morphing (AoS to SoA Remapping & Survival Mode)\",\n", f);
    fputs("        .philosophy_why = \"Code is not carved in stone, but clay morphing in memory. When RAM collapses, the system executes microsecond geometric morphing to escape the OS OOM Killer.\",\n", f);
    fputs("        .book_excerpt = \"Code is not carved in stone, but clay morphing in memory. When RAM collapses, the system executes microsecond geometric morphing to escape the OS OOM Killer.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"token_ring\",\n", f);
    fputs("        .chapter_ref = \"ch12_wavefront_cacheline_topology.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 12: Wavefront Concurrency Ring & alignas(64) Cacheline Topology (Single-Cacheline Atomic Phase Swap)\",\n", f);
    fputs("        .philosophy_why = \"In multi-core concurrency, cross-cacheline tearing is original sin. FLOW constrains wavefront slots to single 64B cachelines with zero spinlock synchronization.\",\n", f);
    fputs("        .book_excerpt = \"In multi-core concurrency, cross-cacheline tearing is original sin. FLOW constrains wavefront slots to single 64B cachelines with zero spinlock synchronization.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"wavefront\",\n", f);
    fputs("        .chapter_ref = \"ch12_wavefront_cacheline_topology.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 12: Wavefront Concurrency Ring & alignas(64) Cacheline Topology (Single-Cacheline Atomic Phase Swap)\",\n", f);
    fputs("        .philosophy_why = \"In multi-core concurrency, cross-cacheline tearing is original sin. FLOW constrains wavefront slots to single 64B cachelines with zero spinlock synchronization.\",\n", f);
    fputs("        .book_excerpt = \"In multi-core concurrency, cross-cacheline tearing is original sin. FLOW constrains wavefront slots to single 64B cachelines with zero spinlock synchronization.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"reload\",\n", f);
    fputs("        .chapter_ref = \"ch13_qsbr_lockfree_hotswap.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 13: QSBR Lock-Free Hot-Swap (Sub-Microsecond Generational Migration)\",\n", f);
    fputs("        .philosophy_why = \"In servers processing millions of requests/sec, acquiring even one RWLock triggers catastrophic cache-line bouncing. FLOW QSBR achieves zero atomic writes on read paths and sub-microsecond migration.\",\n", f);
    fputs("        .book_excerpt = \"In servers processing millions of requests/sec, acquiring even one RWLock triggers catastrophic cache-line bouncing. FLOW QSBR achieves zero atomic writes on read paths and sub-microsecond migration.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"flowy_fvec\",\n", f);
    fputs("        .chapter_ref = \"ch14_fvec_universal_lockfile.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 14: Universal .fvec & Lockfiles (Architecture Weights, Zero-Cold-Start & Antibodies)\",\n", f);
    fputs("        .philosophy_why = \"The brain infers in real time, while .fvec stores long-term architecture weights. It serves as a Universal Lockfile with SMT signatures and an immune antibody repository.\",\n", f);
    fputs("        .book_excerpt = \"The brain infers in real time, while .fvec stores long-term architecture weights. It serves as a Universal Lockfile with SMT signatures and an immune antibody repository.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"fvec\",\n", f);
    fputs("        .chapter_ref = \"ch14_fvec_universal_lockfile.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 14: Universal .fvec & Lockfiles (Architecture Weights, Zero-Cold-Start & Antibodies)\",\n", f);
    fputs("        .philosophy_why = \"The brain infers in real time, while .fvec stores long-term architecture weights. It serves as a Universal Lockfile with SMT signatures and an immune antibody repository.\",\n", f);
    fputs("        .book_excerpt = \"The brain infers in real time, while .fvec stores long-term architecture weights. It serves as a Universal Lockfile with SMT signatures and an immune antibody repository.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"vault\",\n", f);
    fputs("        .chapter_ref = \"ch14_fvec_universal_lockfile.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 14: Universal .fvec & Lockfiles (Architecture Weights, Zero-Cold-Start & Antibodies)\",\n", f);
    fputs("        .philosophy_why = \"The brain infers in real time, while .fvec stores long-term architecture weights. It serves as a Universal Lockfile with SMT signatures and an immune antibody repository.\",\n", f);
    fputs("        .book_excerpt = \"The brain infers in real time, while .fvec stores long-term architecture weights. It serves as a Universal Lockfile with SMT signatures and an immune antibody repository.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"flowy\",\n", f);
    fputs("        .chapter_ref = \"ch15_deterministic_flowy_reasoner.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 15: Deterministic Flowy Reasoner (0% Hallucination Introspection & Decoupling)\",\n", f);
    fputs("        .philosophy_why = \"We do not need a hallucinating probabilistic chatbot to explain system architecture. FLOW built Flowy: a 100% deterministic, zero-hallucination causal codebase reasoner in pure C.\",\n", f);
    fputs("        .book_excerpt = \"We do not need a hallucinating probabilistic chatbot to explain system architecture. FLOW built Flowy: a 100% deterministic, zero-hallucination causal codebase reasoner in pure C.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"flowy_cli\",\n", f);
    fputs("        .chapter_ref = \"ch15_deterministic_flowy_reasoner.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 15: Deterministic Flowy Reasoner (0% Hallucination Introspection & Decoupling)\",\n", f);
    fputs("        .philosophy_why = \"We do not need a hallucinating probabilistic chatbot to explain system architecture. FLOW built Flowy: a 100% deterministic, zero-hallucination causal codebase reasoner in pure C.\",\n", f);
    fputs("        .book_excerpt = \"We do not need a hallucinating probabilistic chatbot to explain system architecture. FLOW built Flowy: a 100% deterministic, zero-hallucination causal codebase reasoner in pure C.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"audit\",\n", f);
    fputs("        .chapter_ref = \"ch15_deterministic_flowy_reasoner.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 15: Deterministic Flowy Reasoner (0% Hallucination Introspection & Decoupling)\",\n", f);
    fputs("        .philosophy_why = \"We do not need a hallucinating probabilistic chatbot to explain system architecture. FLOW built Flowy: a 100% deterministic, zero-hallucination causal codebase reasoner in pure C.\",\n", f);
    fputs("        .book_excerpt = \"We do not need a hallucinating probabilistic chatbot to explain system architecture. FLOW built Flowy: a 100% deterministic, zero-hallucination causal codebase reasoner in pure C.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"benchmark\",\n", f);
    fputs("        .chapter_ref = \"ch16_frontier_pillars_and_control_defenses.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 16: Four Frontier Pillars Benchmark & Control Theory Defenses\",\n", f);
    fputs("        .philosophy_why = \"Four Frontier Pillars stress testing, Schmitt trigger hysteresis anti-flapping, and production-grade real benchmarks.\",\n", f);
    fputs("        .book_excerpt = \"Four Frontier Pillars stress testing, Schmitt trigger hysteresis anti-flapping, and production-grade real benchmarks.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"gateway\",\n", f);
    fputs("        .chapter_ref = \"ch16_frontier_pillars_and_control_defenses.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 16: Four Frontier Pillars Benchmark & Control Theory Defenses\",\n", f);
    fputs("        .philosophy_why = \"Self-evolving edge gateway, million-op SMT WAF formal checks, and high-concurrency surge absorption.\",\n", f);
    fputs("        .book_excerpt = \"Self-evolving edge gateway, million-op SMT WAF formal checks, and high-concurrency surge absorption.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"manifold_algebra\",\n", f);
    fputs("        .chapter_ref = \"ch17_autopoiesis_spacetime_preplay.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 17: Manifold Algebra, Neuro-Bit Bridge, Spacetime Pre-Play & Swarm Autopoiesis\",\n", f);
    fputs("        .philosophy_why = \"Constraint convergence is correlation. Statistical correlation is merely a shadow of physical geometric constraints projected onto observation dimensions.\",\n", f);
    fputs("        .book_excerpt = \"Constraint convergence is correlation. Statistical correlation is merely a shadow of physical geometric constraints projected onto observation dimensions.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"neuro_bridge\",\n", f);
    fputs("        .chapter_ref = \"ch17_autopoiesis_spacetime_preplay.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 17: Manifold Algebra, Neuro-Bit Bridge, Spacetime Pre-Play & Swarm Autopoiesis\",\n", f);
    fputs("        .philosophy_why = \"Constraint convergence is correlation. Statistical correlation is merely a shadow of physical geometric constraints projected onto observation dimensions.\",\n", f);
    fputs("        .book_excerpt = \"Constraint convergence is correlation. Statistical correlation is merely a shadow of physical geometric constraints projected onto observation dimensions.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"spacetime_preplay\",\n", f);
    fputs("        .chapter_ref = \"ch17_autopoiesis_spacetime_preplay.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 17: Manifold Algebra, Neuro-Bit Bridge, Spacetime Pre-Play & Swarm Autopoiesis\",\n", f);
    fputs("        .philosophy_why = \"Constraint convergence is correlation. Statistical correlation is merely a shadow of physical geometric constraints projected onto observation dimensions.\",\n", f);
    fputs("        .book_excerpt = \"Constraint convergence is correlation. Statistical correlation is merely a shadow of physical geometric constraints projected onto observation dimensions.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"swarm_autopoiesis\",\n", f);
    fputs("        .chapter_ref = \"ch17_autopoiesis_spacetime_preplay.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 17: Manifold Algebra, Neuro-Bit Bridge, Spacetime Pre-Play & Swarm Autopoiesis\",\n", f);
    fputs("        .philosophy_why = \"Constraint convergence is correlation. Statistical correlation is merely a shadow of physical geometric constraints projected onto observation dimensions.\",\n", f);
    fputs("        .book_excerpt = \"Constraint convergence is correlation. Statistical correlation is merely a shadow of physical geometric constraints projected onto observation dimensions.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"entropy_collapse\",\n", f);
    fputs("        .chapter_ref = \"ch18_zero_trivia_developer_experience.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 18: Zero-Trivia Developer Experience & Structural Entropy Collapse (Declarative Compiler & Kolmogorov Limit)\",\n", f);
    fputs("        .philosophy_why = \"Lines of code measure system structural entropy K(S). Developers do not need to memorize verbose flags; declare your intent and the compiler synthesizes everything.\",\n", f);
    fputs("        .book_excerpt = \"Lines of code measure system structural entropy K(S). Developers do not need to memorize verbose flags; declare your intent and the compiler synthesizes everything.\"\n", f);
    fputs("    },\n", f);
    fputs("    {\n", f);
    fputs("        .module_id = \"zero_trivia\",\n", f);
    fputs("        .chapter_ref = \"ch18_zero_trivia_developer_experience.md\",\n", f);
    fputs("        .chapter_title = \"Chapter 18: Zero-Trivia Developer Experience & Structural Entropy Collapse (Declarative Compiler & Kolmogorov Limit)\",\n", f);
    fputs("        .philosophy_why = \"Lines of code measure system structural entropy K(S). Developers do not need to memorize verbose flags; declare your intent and the compiler synthesizes everything.\",\n", f);
    fputs("        .book_excerpt = \"Lines of code measure system structural entropy K(S). Developers do not need to memorize verbose flags; declare your intent and the compiler synthesizes everything.\"\n", f);
    fputs("    },\n", f);
    fputs("};\n\n", f);
    fputs("static inline const FlowModuleBookBinding *flow_book_lookup_binding_lang(const char *module_id, FlowLanguage lang) {\n", f);
    fputs("    if (module_id == NULL) return NULL;\n", f);
    fputs("    const FlowModuleBookBinding *bindings = (lang == FLOW_LANG_EN) ? FLOW_MODULE_BOOK_BINDINGS_EN : FLOW_MODULE_BOOK_BINDINGS_ZH;\n", f);
    fputs("    for (size_t i = 0; i < FLOW_BOOK_MODULE_BINDING_COUNT; ++i) {\n", f);
    fputs("        if (strcmp(bindings[i].module_id, module_id) == 0) return &bindings[i];\n", f);
    fputs("    }\n", f);
    fputs("    return NULL;\n", f);
    fputs("}\n\n", f);
    fputs("static inline const FlowModuleBookBinding *flow_book_lookup_binding(const char *module_id) {\n", f);
    fputs("    return flow_book_lookup_binding_lang(module_id, FLOW_LANG_ZH);\n", f);
    fputs("}\n\n", f);
    fputs("static inline const FlowBookChapterDoc *flow_book_lookup_chapter_lang(const char *ref_or_name, FlowLanguage lang) {\n", f);
    fputs("    if (ref_or_name == NULL) return NULL;\n", f);
    fputs("    const FlowBookChapterDoc *chapters = (lang == FLOW_LANG_EN) ? FLOW_BOOK_CHAPTERS_EN : FLOW_BOOK_CHAPTERS_ZH;\n", f);
    fputs("    for (size_t i = 0; i < FLOW_BOOK_CHAPTER_COUNT; ++i) {\n", f);
    fputs("        if (strcmp(chapters[i].chapter_ref, ref_or_name) == 0 || strstr(chapters[i].chapter_title, ref_or_name) != NULL) {\n", f);
    fputs("            return &chapters[i];\n", f);
    fputs("        }\n", f);
    fputs("    }\n", f);
    fputs("    return NULL;\n", f);
    fputs("}\n\n", f);
    fputs("static inline const FlowBookChapterDoc *flow_book_lookup_chapter(const char *ref_or_name) {\n", f);
    fputs("    return flow_book_lookup_chapter_lang(ref_or_name, FLOW_LANG_ZH);\n", f);
    fputs("}\n\n", f);
    fputs("#endif /* FLOW_GENERATED_BOOK_KNOWLEDGE_H */\n", f);
    fclose(f);
    printf("Successfully synchronized %s (18 chapters, 53 module bindings)\n", out_path);
    return 0;
}
